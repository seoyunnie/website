import { createTheme } from "@mantine/core";

export const theme = createTheme({
  primaryColor: "red",
  autoContrast: true,
  fontFamily: "Roboto, sans-serif",
  defaultRadius: "md",
  cursorType: "pointer",
});
